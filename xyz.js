function calculateS(a, b) {
  console.log(a + b);
}
module.exports = calculateS;
// by default node js uses commonJS modules so TO export any thing we have to use module.export = 
// function which we want to export  